import 'package:flutter/material.dart';

class AnalysisChapterPage extends StatelessWidget {
  const AnalysisChapterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
